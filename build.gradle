apply plugin: "java"
apply plugin: "idea"
apply plugin: "application"
apply plugin: "findbugs"

mainClassName = "io.github.tomykaira.dynamicrun"

repositories {
    mavenCentral()
}

dependencies {
}

task wrapper(type: Wrapper) {
    gradleVersion = '1.9'
}
